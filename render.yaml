services:
  - type: web
    name: textfind-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn ocr_server:app --bind 0.0.0.0:$PORT --timeout 120 --workers 2
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.3
